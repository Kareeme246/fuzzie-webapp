import React from "react";

type Props = {};

const Page = (props: Props) => {
  //TODO: If the user tries to access this route send them to the first workflow they have or create one .
  return <div>Page</div>;
};

export default Page;
